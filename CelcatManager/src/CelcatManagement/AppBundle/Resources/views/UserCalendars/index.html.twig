{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserCalendars list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Calendarfile</th>
                <th>Calendarname</th>
                <th>Calendarcomment</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('usercalendars_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.username }}</td>
                <td>{{ entity.calendarFile }}</td>
                <td>{{ entity.calendarName }}</td>
                <td>{{ entity.calendarComment }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('usercalendars_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('usercalendars_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('usercalendars_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

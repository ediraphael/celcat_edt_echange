{% extends 'CelcatManagementAppBundle::layout.html.twig' %}

{% block titre %}
    <h1>Liste des modèles de mail</h1>
{% endblock %}

{% block body -%}

    <div class="table-responsive">
        <table class="records_list  table table-striped table-hover">
            <thead>
                <tr>
                    <th></th>
                    <th>Nom</th>
                    <th>De</th>
                    <th>A</th>
                    <th>Sujet</th>
                    <th>Message</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for entity in entities %}
                    <tr>
                        <td>
                            <div>
                                {% include 'Action/action-visualiser.html.twig' with { 'href' :  path('celcat_management_app_modelmail_show', { 'id': entity.id }) } %}
                                {% include 'Action/action-modifier.html.twig' with { 'href' :  path('celcat_management_app_modelmail_edit', { 'id': entity.id }) } %}
                            </div>
                        </td>
                        <td>{{ entity.name }}</td>
                        <td>{{ entity.fromAddress }}</td>
                        <td>{{ entity.toAddress }}</td>
                        <td>{{ entity.subject }}</td>
                        <td>{{ entity.body }}</td>
                        <td>{% include 'Action/action-supprimer.html.twig' with { 'href' :  path('celcat_management_app_usermail_delete', { 'id': entity.id }) } %}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
